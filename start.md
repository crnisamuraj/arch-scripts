<!--meta install.sh to run all module's/folders install.sh script
every folder is module and can implement scripts from main (install, uninstall, setup, configure, etc)
to write module you just add another folder with required scripts named install, uninstall, setup, configure, similar, like command/hook module implments, all are optional to implement for module.-->
